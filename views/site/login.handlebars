<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="../css/index.css">
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" 
   integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" 
   crossorigin="anonymous" referrerpolicy="no-referrer" />
   <link rel="icon" href="/img/favicon/logo.png" type="image/png">
   <title>by Şükrü</title>
</head>
<body>

    <div class="main-area">
        <a href="/">X</a>
    </div>


    <div class="register">
        <div class="register-left"></div>
        <div class="register-center">
            <div>
                <h1>Giriş Yap</h1>
            </div>

            <div class="login-area" id="loginArea">
                <a href="/register" id="loginText">Hala bir hesabınız yok mu?</a>
            </div>

            <form>
                <div class="form-group">
                    <input type="text" name="email" required>
                    <span>E-mail *</span>
                </div>

                <div class="form-group">
                    <input type="text" name="password" required>
                    <span>Şifre *</span>
                </div>

                <div class="button-group">
                    <button id="register">Giriş</button>
                </div>
            </form>
        </div>
        
        <div class="register-right">
            <div class="message-register" id="registerMessageArea">
                <p id="registerMessage"></p>
            </div>
        </div>
        

    </div>

    <script src="/js/scroll.js"></script>
    <script>
        const registerButton = document.getElementById('register');  
        const form = registerButton.closest('form'); // Formu bul
        const registerMessageArea = document.getElementById('registerMessageArea');
        const loginArea= document.getElementById('loginArea');
        const loginText=document.getElementById('loginText');

        registerButton.addEventListener('click', (e) => {
            e.preventDefault(); // Formun varsayılan gönderimini engelle  

            const formData = new FormData(form); // Form verilerini al
            
            fetch('/login', {
                method: 'post',  
                body: formData
                
            }).then(response => {  
                if (!response.ok) {  
                    throw new Error('Ağ yanıtı uygun değil: ' + response.status);  
                }  
                return response.json();  
            }).then(res => {  
                //console.log(res);  
                if (res.case) {  
                    window.location.href = '/'; // Kayıt başarılıysa anasayfaya yönlendir
                    return;
                } else {
                    if(!res.case && res.message==='Şifre girişi hatalı'){
                        registerMessage.textContent = 'Şifre girişi hatalı! Lütfen kontrol edip tekrar deneyiniz';  
                        registerMessageArea.classList.add('visible');
                        return;
                    } else if(!res.case && res.message==='Mail bilgisi eksik veya hatalı'){
                        registerMessage.textContent = 'Mail bilgisi eksik veya hatalı! Lütfen kontrol edip tekrar deneyiniz';
                        registerMessageArea.classList.add('visible');
                        return;
                    } else if(!res.case && res.message==='Eksik bilgi girildi'){
                        registerMessage.textContent = 'Eksik giriş yapıldı! Lütfen kontrol edip tekrar deneyiniz';
                        registerMessageArea.classList.add('visible');
                        return;
                    } else if(!res.case && res.message==='Kayıt bulunamadı'){
                        registerMessage.textContent = 'Kaydınız bulunamadı. Kayıt ol sayfasından giriş yapınız';
                        registerMessageArea.classList.add('visible');
                        loginArea.classList.add('change');
                        loginText.textContent='Yeni hesap oluştur';
                        return;
                    }
                }
                
            }).catch(error => {  
                console.error('Hata:', error);  
            });  
        });  
    
    </script>

</body>
</html>